%section
  .wrapper-page
    .tutor-popup.popup
      .tutor-text
        %a.popup-close(href="#" )close
          %img(src="#{$this->assetsPath}/img/close-button.png" )
        .popup-loading
          %img(src="#{$this->assetsPath}/img/preloader_small.gif")
        .popup-content
    .wrapper-tutors-page
      %h1 Our tutors
      .tutor-details-wrap
        -$i = 1
        -foreach($tutors as $tutor)
          .tutor-details(style="#{$i == 1 ? 'display:block;': 'display:none;';}" data-rel="tutor#{$tutor->id}")
            .tutor-details-image
              %img(src="#{$tutorsImgFolder . $tutor->big_image}" )
            .tutors-info
              %h2 = $tutor->name
              .subjects
                .green-point
                  Subjects:
                %br
                  %p
                    %span = $tutor->subjects
              %br
              .formation
                .green-point
                  Education:
                %br
                  %p = $tutor->education
              %br
              .experience
                .green-point
                  Experience:
                %br
                %p = $tutor->experience
              .days-times-wrapper
                .green-point
                  Days/times available:
                %br
                %ul.days-times
                  -$dayTimeCount = count($tutor->tutorsDaysTimes)
                  -foreach($tutor->tutorsDaysTimes as $dayTime)
                    %li(style="width:#{100/$dayTimeCount}%;")
                      %span = $weekDays[$dayTime->weekday]
                      = date('gA', strtotime($dayTime->start_time)) . '-' .date('gA', strtotime($dayTime->end_time))
                  %input(type="button" class="btn our-tutors-button contact-tutor" value="contact now" data-popup-open="tutor-popup" data-tutor-id="#{$tutor->id}")
            .clear
            -$i++
      .other-tutors
        %h2 Other tutors
        .tutors-carousel
          .slider_nav.prev_
          .slider-list
            .slider-wrap
              -$j = 1
              -foreach($tutors as $tutor)
                .teachers(id="tutor#{$tutor->id}")
                  .teachers-image(class="#{$j%2 ? 'even': 'odd';}")
                    %a(href="#" )
                      %img(class="tutor-preview" src="#{$tutorsImgFolder . $tutor->small_image}" )
                  %h2 = $tutor->name
                  .subjects
                    .green-point
                      Subjects:
                    %br
                    %p
                      %span = $tutor->subjects
                  .time-avalibe-wrapper
                    .green-point
                      Days/times available:
                    %ul
                      -foreach($tutor->tutorsDaysTimes as $dayTime)
                        %li = $weekDays[$dayTime->weekday]. ' ' . date('gA', strtotime($dayTime->start_time)) . '-' .date('gA', strtotime($dayTime->end_time))
                  %a(class="full-info" href="#" ) Watch full info
                    %img(src="#{$this->assetsPath}/img/button_more.png" )
                    .clear
                  %input(type="button" class="btn all-buttons contact-tutor" value="contact now" data-popup-open="tutor-popup" data-tutor-id="#{$tutor->id}")
                -$j++
            .clear
          .slider_nav.next_
      .clear